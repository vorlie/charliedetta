<astro>
<div class="bg">
    <nav class="navbar">
      <ul class="nav-list">
        <li class="nav-item"><a href="/">Home</a></li>
        <li class="nav-item"><a href="/search">Search</a></li>
        <li class="nav-item"><a href="/materialdetta">M3 Gen</a></li>
      </ul>
    </nav>        
    <div class="vve1">
        <img src="https://cdn.discordapp.com/avatars/670986272377929743/d85e76f13346b2e754e4f8bc3e8c08ee.png" width="40">
        <p>@vve1</p>
    </div>
</div>
</astro>
  
<style>
    .bg{
        overflow: hidden;
    }
    .navbar {
        float: right;
        overflow: hidden;
        border-radius: 16px;
        border: 2px solid var(--color-1);
        background-color: var(--color-main-background);
        -webkit-backdrop-filter: blur(40px);
        backdrop-filter: blur(5px);
        padding: 1rem;
    }
  
    .nav-list {
        list-style-type: none;
        display: flex;
        margin: 0;
        padding: 0;
    }
  
    .nav-item {
        padding: 0.5rem;
        font-weight: 600;
		font-size: 20px;
    }
  
    .nav-item a {
        text-decoration: none;
        color: #e4e4e4;
        transition: 0.2s ease;
    }
    .nav-item a:hover{
        background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
    }
    .vve1{
        float: left;
        padding: 1rem;
        border-radius: 16px;
        border: 2px solid var(--color-1);
        background-color: var(--color-main-background);
        -webkit-backdrop-filter: blur(40px);
        backdrop-filter: blur(5px);
		overflow: hidden;
		display: flex;
		text-align: center;
		align-items: center;
		justify-content: center;
	}
    .vve1 img{
		border-radius: 50%;
		float: left;
		margin-right: 15px;
	}
	.vve1 p{
        padding: 0.5rem;
        margin: auto;
		font-weight: 600;
        background-image: var(--accent-gradient);
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
		float: left;
		font-size: 20px;
	}
    @media (max-width: 569px) {
        .navbar{
            float: left;
            margin-bottom: 8px;
        }
    }
</style>